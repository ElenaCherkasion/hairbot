"scripts": {
  // ... существующие скрипты ...
  "backup:list": "node scripts/backup-db.js --list",
  "backup:restore": "node scripts/backup-db.js --restore",
  "logs:clear": "rm -f logs/*.log && echo 'Логи очищены'",
  "logs:view": "tail -f logs/*.log",
  "stats:detailed": "node scripts/show-stats.js --detailed",
  "check:all": "npm run check-env && npm run check-health && npm run stats"
}
